Django==4.2.8
djangorestframework==3.14.0
django-cors-headers==4.3.1
gunicorn==21.2.0
requests==2.31.0
# The ML packages below are intentionally left unpinned rather than
# exact-pinned. Two environments need to install this file and they
# need different real versions:
#   - Linux/Python 3.9 (Docker, verified 2026-09-02): needs OLD pins —
#     sentence-transformers==2.2.2, huggingface_hub==0.20.3,
#     transformers==4.35.2, torch==2.1.1, scikit-learn==1.3.2,
#     pandas==2.1.3, numpy==1.26.2 — because 2.2.2 calls
#     huggingface_hub.cached_download, removed in huggingface_hub 0.36+.
#   - Windows/Python 3.13 (verified 2026-09-22, RTX 5060 Ti): none of
#     those old versions have prebuilt wheels for 3.13 at all — pip
#     resolved sentence-transformers==6.1.0, huggingface_hub==1.32.0,
#     transformers==5.17.0, torch==2.14.0 (or torch+cu128 for GPU),
#     scikit-learn==1.8.0, pandas==3.0.6 instead, and those work fine
#     together since they're all current releases.
# A single exact pin can't satisfy both. Left unpinned until this has
# its own resolution (e.g. separate requirements files per platform).
sentence-transformers
huggingface_hub
transformers
torch
scikit-learn
pandas
numpy
joblib==1.3.2
psycopg2-binary
redis==5.0.1
celery==5.3.4
python-dotenv==1.0.0
PyMuPDF==1.25.4
python-docx==1.1.0
