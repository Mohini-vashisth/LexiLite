Django==4.2.8
djangorestframework==3.14.0
django-cors-headers==4.3.1
gunicorn==21.2.0
requests==2.31.0
sentence-transformers==2.2.2
# sentence-transformers==2.2.2 calls huggingface_hub.cached_download, a
# function later removed from huggingface_hub (present in 0.2x, gone by
# 0.36.2 — confirmed by hitting this exact ImportError in a real container
# build on 2026-09-02). Pinning below that removal point until
# sentence-transformers itself is upgraded to a version that no longer
# calls the removed function.
huggingface_hub==0.20.3
transformers==4.35.2
torch==2.1.1
scikit-learn==1.3.2
pandas==2.1.3
numpy==1.26.2
joblib==1.3.2
psycopg2-binary==2.9.9
redis==5.0.1
celery==5.3.4
python-dotenv==1.0.0
