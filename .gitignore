# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.env
*.sqlite3
.DS_Store

# Jupyter
.ipynb_checkpoints

# Model files
/saved_model_cuad_model/
/saved_model_ledgar_model/

/data/*.csv

# VSCode
.vscode/

#CUAD
cuad/

# Stock pretrained weights (476MB) — not a project artifact, just the
# public roberta-base checkpoint used as a frozen encoder. Re-downloads
# automatically via transformers on first use; keeping it out of git
# avoids GitHub's 100MB file-size limit and Git LFS entirely.
train_model/saved_model_roberta/model.safetensors